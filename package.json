{
  "name": "ostm",
  "version": "0.1.0",
  "description": "OSTM - Open SSH Tunnel Manager. Backend REST to administrate, supervise and control SSH port-forwarding tunnels.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "license": "MIT",
  "dependencies": {
    "@fastify/websocket": "^11.0.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "fastify": "^5.0.0",
    "jsonwebtoken": "^9.0.2",
    "pino": "^9.4.0",
    "ssh2": "^1.15.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "pino-pretty": "^11.2.2",
    "supertest": "^7.0.0",
    "vitest": "^2.1.0"
  }
}
